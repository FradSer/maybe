---
en:
  activerecord:
    errors:
      models:
        entry:
          attributes:
            base:
              currency_mismatch: Trade must be in the same currency as its entry
              invalid_sell_quantity: cannot sell %{sell_qty} shares of %{ticker} because
                you only own %{current_qty} shares

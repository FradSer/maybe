<%# locals: (avatar_url: nil, initials: "U", lazy: false) %>

<% if avatar_url.present? %>
  <%= image_tag avatar_url, class: "rounded-full w-full h-full object-cover", loading: lazy ? "lazy" : "eager" %>
<% else %>
  <div class="w-full h-full bg-surface-inset hover:bg-surface-inset-hover text-gray-600 theme-dark:text-gray-400 rounded-full flex items-center justify-center text-lg uppercase"><%= initials %></div>
<% end %>

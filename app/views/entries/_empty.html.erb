<div class="flex flex-col items-center justify-center py-20" data-empty-state>
  <p class="text-secondary mb-2"><%= t(".title") %></p>
  <p class="text-subdued max-w-xs text-center mb-4"><%= t(".description") %></p>
  <%= render DS::Link.new(
    text: "New transaction",
    icon: "plus",
    href: new_transaction_path,
    frame: :modal,
    variant: "primary"
  ) %>
</div>
